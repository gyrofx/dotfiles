# Unix
alias ll="ls -alh"
alias ln="ln -v"
alias mkdir="mkdir -p"
alias e="$EDITOR"
alias v="$VISUAL"

# git-easy-checkout
alias geco="git-easy-checkout"

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

alias dotfiles='/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'